[project]
name = "kyber-CS"
version = "0.1.0"
description = "Computer Science, from bits to distributed systems — built for the cybernetics stack"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "jupyter-book>=2.1.6",
    "mystmd>=1.10.1",
]
